# retry-lib

A lightweight Go utility for retrying unreliable operations with configurable attempts and exponential backoff.

## Features

- Simple retry mechanism
- Configurable number of attempts
- Exponential backoff support
- Stops immediately on success
- Returns last error if all retries fail

## Installation

```bash
go get github.com/YOUR_USERNAME/retry-lib